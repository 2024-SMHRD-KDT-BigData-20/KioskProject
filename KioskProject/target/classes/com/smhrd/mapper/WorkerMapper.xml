<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.smhrd.mapper.WorkerMapper">
    
    <!-- 관리자 로그인 -->
    <select id="worker_login" parameterType="com.smhrd.entity.Worker" resultType="com.smhrd.entity.Worker">
        select * from worker where work_id=#{work_id} and work_pw=#{work_pw}
    </select>

</mapper>